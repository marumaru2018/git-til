# React でコンポーネントが再実行（再描画）されるタイミング

- state の状態が変化した場合
  ステートとして定義した値が更新された場合、対象のコンポーネント全体が再描画されます

- コンポーネントに渡された props の値が更新された場合
  コンポーネントの引数である Props が更新された場合も対象コンポーネントは再描画されます

- 親コンポーネントが再描画された場合
  親コンポーネントが再描画されると、そこから呼び出される子コンポーネントも再描画されます
