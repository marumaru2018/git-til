# React で、画像ファイルを扱う

public フォルダ内にある画像は、<img>タグで URL を指定する

```
<img src="/camp-coffee.jpg" />
```

src フォルダ内にある画像ファイルは、import して src 属性に指定する

```
import campCoffee from "./assets/camp-coffee.jpg";

<img src={campCoffee} />;
```
